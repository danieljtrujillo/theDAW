{
  "name": "thedaw-questcast",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "theDAW Quest cast sidecar: mirrors an ADB-connected headset/Android device over the scrcpy protocol and relays the raw H.264 video stream to the browser over WebSocket (decoded there with WebCodecs). No terminal, no external scrcpy app.",
  "scripts": {
    "start": "node server.mjs",
    "postinstall": "node node_modules/@yume-chan/fetch-scrcpy-server/bin/fetch-server.js 3.3.3"
  },
  "dependencies": {
    "@yume-chan/adb": "^2.6.0",
    "@yume-chan/adb-scrcpy": "^2.3.2",
    "@yume-chan/adb-server-node-tcp": "^2.5.2",
    "@yume-chan/scrcpy": "^2.3.0",
    "@yume-chan/stream-extra": "^2.6.1",
    "@yume-chan/fetch-scrcpy-server": "^1.0.0",
    "ws": "^8.18.0"
  }
}
